# assets

Static files imported directly into components — images, SVGs, logos, fonts (e.g. `pl_logo.svg`).

Keep out of `assets/`:
- Data that represents app content/state (e.g. team/fixture data) — belongs with the data layer, not here
